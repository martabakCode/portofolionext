/**
 * Core Systems - Barrel Export
 * 
 * Usage:
 * import { gameManager, eventBus, interactionSystem } from '@/core';
 */

export { eventBus, EventBus } from './EventBus';
export { gameManager, GameManager } from './GameManager';
export { worldManager, WorldManager } from './WorldManager';
export { interactionSystem, InteractionSystem } from './InteractionSystem';
export { default as GameProvider } from './GameProvider';
